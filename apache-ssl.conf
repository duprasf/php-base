<IfModule mod_ssl.c>
# The entire file was removed so it will not REQUIRE a certificate to be used
# the full file that you should use for your production site can be found at
# https://github.hc-sc.gc.ca/hs/php-base-docker/blob/master/apache-ssl.conf.dist
</IfModule>
